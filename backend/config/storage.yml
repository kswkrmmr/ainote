test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

# Cloudflare R2 はS3互換なので、AWS S3のサービスにR2のエンドポイントを指定して使う。
# R2はリージョンの概念がないため region は "auto" 固定。
# 環境変数は Render のダッシュボードで設定する。
cloudflare:
  service: S3
  access_key_id: <%= ENV["R2_ACCESS_KEY_ID"] %>
  secret_access_key: <%= ENV["R2_SECRET_ACCESS_KEY"] %>
  endpoint: <%= ENV["R2_ENDPOINT"] %>
  bucket: <%= ENV["R2_BUCKET"] %>
  region: auto
  # R2はパス形式のURL(<endpoint>/<bucket>/<key>)のみ対応
  force_path_style: true
  # aws-sdk-s3 はアップロード時に x-amz-sdk-checksum-algorithm / x-amz-checksum-crc32 を
  # 自動付与するが、R2はこれに対応しておらず500になる。AWSが要求する場合のみ送るようにする。
  # 整合性チェックは Active Storage が付ける Content-MD5 で担保される。
  request_checksum_calculation: when_required
  response_checksum_validation: when_required
